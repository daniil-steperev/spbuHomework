package group144.stepyrev;

/** An interface that intended for sorting an array of integers. */
public interface Sorter {
    /**
     * A method that sorts an array.
     * @param sortedArray - an array that should be sorted
     */
    void sort(int[] sortedArray);
}
